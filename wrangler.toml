name = "naf-marketing"
main = "src/index.ts"
compatibility_date = "2024-06-01"
compatibility_flags = ["nodejs_compat"]

# يخدم هذا الـ Worker واجهة React (assets) بالإضافة إلى الـ API عبر Hono
[assets]
directory = "./web/dist"
binding = "ASSETS"
not_found_handling = "single-page-application"
# بدونه تخدم طبقة الأصول index.html قبل تشغيل الـ Worker، فيصل الزائر إلى
# الجذر دون المرور بحارس الدخول الموحّد إطلاقاً — واللوحة تُحمَّل له كاملةً
# ثم تفشل نداءاتها وحدها. الحارس يجب أن يسبق كل شيء، وهذا ما يجعله يسبقه.
run_worker_first = true

# جلسات الدخول الموحّد وكاش مفاتيح المركز.
# مفتاحان لا ثالث: sess:{sid} بعمر ما بقي من الرمز، وjwks:{issuer} بخمس دقائق.
# ولا مفتاح حالة عابرة — المركز يولّد state ويخزّنه مع رمز العبور.
#
# والمعرّف يُلتزَم هنا لا في اللوحة: النشر من Workers Builds يقرأ هذا الملف
# ويجعل ما فيه هو الحقيقة، فربطٌ يُضاف من Settings ← Bindings يمحوه أوّل نشر.
# وهو عنوان مساحة لا مفتاح وصول، فلا سرّ فيه.
[[kv_namespaces]]
binding = "AUTH_KV"
id = "44eaef28adf84b11ad1fda916801a416"

# قاعدة بيانات D1
[[d1_databases]]
binding = "DB"
database_name = "naf_marketing"
database_id = "4ab835ee-5c3d-477f-be4b-9def4f49e06a"
migrations_dir = "migrations"

# تصيير المتصفح — لتوليد PDF من صفحة المقالة.
# يحتاج خطة Workers مدفوعة. وبدونه تعمل المنصة كاملةً: زرّ PDF يختفي
# ويبقى «نسخة للطباعة»، والشيفرة تفحص env.BROWSER ولا تفترضه.
[browser]
binding = "BROWSER"

# تخزين الوسائط R2
[[r2_buckets]]
binding = "MEDIA"
bucket_name = "naf-marketing-media"

# المهام المجدولة:
# مشغّل واحد فقط (حساب Cloudflare محدود بـ ٥ مهام cron إجمالاً) — التوقيت يُدار داخل الكود:
#   "*/2 * * * *" كل دقيقتين، ولكل مهمةٍ ثقيلة دقيقتُها كي تنال حصّة استدعائها من
#                 الطلبات كاملة — الجدول في `src/cron.ts` وفي README.
# النشر لم يعد تلقائياً — يتم يدوياً بالضغط على «نشر الآن»، ومن يفوت موعده يظهر «متأخر».
[triggers]
crons = ["*/2 * * * *"]

# متغيّرات غير سرية (يمكن تعديلها). المفاتيح السرية عبر: wrangler secret put
[vars]
APP_TIMEZONE = "Asia/Riyadh"
APP_NAME = "منصة ناف للتسويق"

# خطة كلاودفلير — `paid` ترفع حصص مزامنة الصندوق والتحليلات وسحب السجلّ
# (`src/services/limits.ts`). ومن نزل إلى المجانية يحذفها أو يجعلها `free`.
# والاحتياط في الكود لا هنا: دورةٌ تسقط عند حدٍّ ما تُنزل الحصص إلى المجانية
# يوماً كاملاً وحدها، دون أن ينتظر أحداً يعدّل هذا السطر.
WORKERS_PLAN = "paid"

# الدخول الموحّد عبر naf-id. هذا القسم وحده هو ما يختلف بين منصة وأخرى.
PLATFORM_ID = "naf-marketing"
AUTH_ISSUER = "https://app.naflaw.sa"

# مخطّط جدول الأعضاء: المنصة لها users قائم لا members جديد.
MEMBERS_TABLE = "users"
MEMBERS_ID_COLUMN = "id"
MEMBERS_NAME_COLUMN = "name"
MEMBERS_ROLE_COLUMN = "role_name"
MEMBERS_TIME_FORMAT = "iso"
# الصلاحيات الدقيقة في مصفوفة roles_permissions لا في عمود — و`-` تعني: لا عمود.
MEMBERS_PERMS_COLUMN = "-"
# أقلّ الأدوار الثلاثة صلاحية، فأول دخول لا يمنح شيئاً حتى يرقّيه المدير العام.
DEFAULT_ROLE = "writer"

# المفاتيح السرية تُضبط عبر Secrets ولا تُخزَّن هنا:
#
#   wrangler secret put <NAME>
#
# وقائمتها كاملةً في `.dev.vars.example` — هناك وحدها. وكانت مسرودةً هنا
# أربعةً من أربعةٍ وعشرين، فقرأها من ضبطها وظنّها الكلّ.
#
# ونوع `AUTH_CLIENT_SECRET` في لوحة كلاودفلير Secret لا Text: بدونه لا
# دخول أصلاً، ووضعُه متغيّراً نصّياً يجعله مقروءاً في اللوحة.

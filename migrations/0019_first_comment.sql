-- أول تعليق لكل منصة — يُنشر تلقائياً بعد المنشور (يُستخدم عادةً للروابط والوسوم
-- كي تبقى خارج متن المنشور). يُخزَّن مع نسخة المنصة لأن النشر يتم لكل منصة على حدة.
ALTER TABLE post_variants ADD COLUMN first_comment TEXT;

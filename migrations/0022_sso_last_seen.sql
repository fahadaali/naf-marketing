-- الدخول الموحّد عبر naf-id: عمود آخر ظهور للعضو.
-- الهجرات للأمام فقط — إضافة عمود لا إعادة بناء جدول.
--
-- ولا عمود sub هنا: users.id نفسه يصير sub بعد الترحيل الكسول،
-- فلا جدول أعضاء ثانٍ ولا هوية مزدوجة في منصة واحدة.
ALTER TABLE users ADD COLUMN last_seen_at TEXT;

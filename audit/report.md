# تقرير الربط بسجلّ ناف — naf-marketing

المنصة: `naf-marketing` · السجلّ: `fahadaali/naf-ui#v1.1.1` · الحوكمة: `naf-governance#v1.1.1`
التاريخ: 2026-07-26 · الفرع: `claude/app-development-1wex1a`

القرار الحاكم لهذه الجلسة (منك): **تبنّي Tailwind v4 (الخيار أ)، وما لا مقابل له في السجلّ يُترك ويُتجاوز.**

عشر دفعات، حفظ منفصل لكل دفعة، كل واحدة قابلة للتراجع وحدها.

---

## ١) ما استُبدل — بالأرقام

| الدفعة | الفئة | ما تغيّر |
|---|---|---|
| ٠ | الأساس | تبنّي Tailwind v4 (‏`tailwindcss@4` + `@tailwindcss/vite`)، نسخ `naf-theme.css` و`naf-font.css` حرفياً، استضافة IBM Plex Sans Arabic ذاتياً، مبدّل السمة يضبط صنف `dark` مع سمة `data-theme` |
| ١ | الألوان | حذف **٢٦** تعريف رمز لوني محلي · تحويل **٢٠٩** استهلاكاً من `hsl(var(--x))` إلى `var(--x)` في **٩** ملفات · الشفافيات إلى `color-mix(in oklab, …)` |
| ٢ | الخطوط | العائلة إلى `var(--font-sans)` · حذف روابط Google Fonts · ارتفاع السطر ١٫٦ إلى ١٫٨ من الثيم · الوزن ٨٠٠ إلى ٧٠٠ |
| ٣ | المسافات والانحناء | حذف `--radius` المحلي · توحيد **٤٢** قيمة انحناء على `--radius` ومشتقاته · تقريب قيم المسافة البعيدة بكسل واحد عن سلّم ٤ |
| ٤ | الظلال | **١٢** تعريف ظل يدوي إلى `--shadow-xs/sm/md/lg/xl/2xl` · توحيد حلقتَي التركيز في واحدة · حذف `--shadow-color` المحلي |
| ٥ | الاتجاه | **٦** مواضع `text-align: right` إلى `start` · **١٣** خاصية فيزيائية في JSX إلى منطقية · نقطة الإشعارات إلى `inset-inline-end` · قلب `Send` و`LogOut` و`ExternalLink` في RTL |
| ٦ | الأيقونات | `📎` إلى `Paperclip` · `←` في زر «رجوع» إلى `ArrowRight` |
| ٧ | المكوّنات | تركيب شعار ناف: **٥** ملفات SVG + مكوّن `NafLogo` من السجلّ + favicon (لم يكن للمنصة شعار ولا أيقونة تبويب) |
| ٨ | النصوص | «الداشبورد» إلى «لوحة التحكم» · «تحرير» إلى «تعديل» · «استرجاع» إلى «استعادة» (**٦** مواضع) · «ابحث» إلى «بحث» · **٤** مواضع «لا توجد بيانات» الممنوعة إلى شاشات فارغة تدعو للعمل |
| ٩ | التنسيق | نسخ `naf-format` · **٥** مواضع أرقام هندية إلى غربية · التواريخ من هجري بأسماء الشهور إلى `2026/07/20` · الوقت من «2:00 م» إلى «14:00» · عزل اتجاهي لكل تاريخ ورقم |

**الحصيلة:** ٣١ ملفاً · ‎+1868 / ‎−324 سطراً · ١٠ حفظات.

---

## ٢) ما تُرك عمداً — ولماذا

### بناءً على قرارك «ما لا مقابل له يُترك»

| المتروك | العدد | السبب |
|---|---|---|
| عائلة `*-soft` | ٥ رموز | ثيم ناف لا يوفّر خلفيات ناعمة للحالات |
| `--purple` و`--purple-soft` | رمزان | لا مقابل — تُستعمل في تدرّج الزر المميّز وشريط التقدّم والشارة الأرجوانية |
| ألوان منصات التواصل | ١١ قيمة | هويات مملوكة لأطراف أخرى؛ تغييرها يُفقد التعرّف على الأيقونات |
| ظلّان ملوّنان بلون العلامة | ٢ | سلّم ناف لا يوفّر ظلاً ملوّناً |
| النجوم `★` `☆` | ٤ مواضع | `naf-icons.md` لا يحوي مفهوم التقييم إطلاقاً |
| `💡` التلميح · `→` سجلّ الاعتماد · `←` فاصل المسار · `←` فاصل النطاق | ٤ | معانيها غير مسجّلة في `naf-icons.md` |
| رفع `lucide-react` من `^0.408.0` إلى `1.27.0` | — | `CLAUDE.md §3` يمنع الرفع في مستودع واحد ويشترط `/verify-icons` على الستة أولاً |
| `XGlyph` (SVG يدوي لشعار إكس) | ١ | Lucide لا يوفّر شعار إكس؛ الاستثناء مبرَّر نصّاً |
| أسهم `DatePicker` | ٢ | مضبوطة يدوياً لـ RTL أصلاً، وقلبها انحدار لا إصلاح |
| موضع `Popover` المحسوب بـ `left` | ١ | إحداثيات شاشة محسوبة بجافاسكربت — تغييره يمسّ السلوك لا الشكل |
| مضاعفات ٤ البعيدة بكسلين (‏6 · 10 · 14 · 18 · 22) | — | التقريب تعادل بين خيارين ويغيّر الكثافة البصرية |

### تُركت لسبب دلالي لا لغياب مقابل

- **«سجل الموافقة»** في صفحة المشتركين: المقصود موافقة الاشتراك النظامية لا `Approve`، فلا ينطبق عليها «اعتماد».
- **«سجل التدقيق»**: `naf-terms` يحوي `Activity log → سجلّ النشاط` وهو مفهوم أضيق من سجلّ التدقيق. لم أبدّله.

### خارج نطاق الجرد

صفحات المقالات العامة المُخدَّمة من الخادم (`src/routes/publicPages.ts`) وقوالب البريد (`src/services/newsletterSend.ts`) تحمل أنماطاً سطرية خاصة بها. الجرد نصّ على النطاق `web/src/**` فقط. **لم تُمَس** — البريد خصوصاً لا يدعم رموز CSS فيلزمه بقاء القيم الخام.

---

## ٣) قرارات ما زالت تحتاج قرارك

1. **عائلة `*-soft`**: تُسجَّل في `naf-ui`؟ أم تُشتقّ من رمزها الأساس بـ `color-mix`؟
   ⚠️ **الأهم في القائمة:** القيم الحالية مشتقّة من النيلي القديم، والأساسي صار ذهبياً. الشارات الناعمة اليوم **بلون عائلة أخرى غير لون العلامة**. يظهر ذلك في شارات الحالة وبطاقات اللوحة.
2. **`--purple`**: حالة دلالية أم زخرفة؟ لو زخرفة فمقابله `--chart-*`.
3. **ألوان المنصات الخارجية**: استثناء مسجَّل؟ أم رموز `--brand-linkedin` وأخواتها في السجلّ؟ (أرشّح الثاني.)
4. **نمط عرض التقييم**: نص مع رمز أم مقياس خمس أيقونات؟ يُسجَّل في `naf-icons.md` قبل التطبيق.
5. **التلميح**: `Info` أم `Lightbulb`؟
6. **مكوّنات السجلّ** (`button` · `card` · `input` · `table` · `dialog` · `alert`): لم يُخرَج لها جدول مطابقة في المرحلة ٣، واستبدالها يمسّ ٣١ ملفاً. تحتاج جلسة مخصّصة بجدول معتمد.
7. **`--danger`** في `Editor.tsx:262`: مرجع **غير معرَّف أصلاً** (لا في الثيم القديم ولا الجديد) بجانب `background: '#fdf3f2'` خام. مطابقته بـ `--destructive` متوسطة الثقة فلم أطبّقها — لكنه اليوم حدّ لا لون له.
8. **إصدار `lucide-react`**: يحتاج `/verify-icons` على الستة ثم رفعاً متزامناً.

---

## ٤) ما أُضيف إلى السجلّ في هذه الجلسة

**لا شيء.** كل ما طُبّق كان له مقابل مسجَّل مسبقاً في `naf-theme.css` أو `naf-icons.md` أو `naf-terms.md` أو `registry/naf/`. وكل ما لم يكن له مقابل تُرك ولم يُجتهد فيه — وهو ما تقتضيه قاعدة «السجلّ يسبق المنصة».

---

## ٥) فروق بصرية قد تكون انحداراً

| الفرق | التقدير |
|---|---|
| الأساسي من نيلي `#4F46E5` إلى ذهبي ناف | **مقصود** — هوية ناف |
| الشارات الناعمة ما زالت نيلية بجوار ذهبي | **انحدار قائم** — انظر القرار ١ |
| ظلال ناف أخفّ من الظلال المحلية السابقة (البطاقات والنوافذ أقلّ ارتفاعاً) | مقصود — سلّم السجلّ |
| ‏`--radius` من 0.65rem إلى 0.5rem — الزوايا أحدّ | مقصود — قرار جمالي مسجَّل في الثيم |
| ارتفاع السطر ١٫٨ رفع ارتفاع البطاقات | مقصود — مساحة رأسية للعربية |
| التواريخ من هجري («٦ صفر ١٤٤٨ هـ») إلى ميلادي («2026/07/20») | مقصود — `naf-terms §5`. **لو أردت المزدوج فالدالة `formatDualDate` جاهزة.** |

---

## ٦) تعريف الإنجاز — نتيجة كل بند بصدق

| # | البند | النتيجة |
|---|---|---|
| 1 | لا قيم خام في الفرق | **جزئي.** `#fff` في ١٠ مواضع (لون رمز فوق خلفية علامة — منخفض الثقة، متروك)، و`#fdf3f2`/`#1f2430` (بطاقة الرفض ومعاينة البريد). لا `rgb(`/`rgba(` جديدة. |
| 2 | لا خصائص اتجاه فيزيائية | **✅ نظيف.** الفرق لا يحوي `ml-`/`mr-`/`pl-`/`pr-`/`text-left`/`text-right` ولا `marginLeft`/`marginRight`/`paddingRight`/`borderRight`. |
| 3 | لا إيموجي | **جزئي.** الاثنان عاليا الثقة استُبدلا؛ ٨ منخفضة الثقة باقية بقرارك، و٣ داخل تعليقات برمجية (ليست واجهة). |
| 4 | العرض في `rtl` و`ltr` | **جزئي — فُحص RTL فقط.** المنصة تثبّت `dir="rtl"` على عنصر الجذر ولا تدعم LTR أصلاً. لم أختبر LTR ولم أدّعِ ذلك. |
| 5 | الوضعان الفاتح والداكن | **✅ فُحص فعلياً.** ٨ صفحات (لوحة التحكم · المحتوى · التحليلات · التعليقات · النشرات · الإعدادات · التقويم · المحرر) في الوضعين، على خادم محلي بقاعدة بيانات حقيقية. |
| 6 | التركيز مرئي عند كل محطة | **✅ فُحص فعلياً.** تنقّل بالمفتاح عبر ١٤ محطة: روابط القائمة عليها `outline`، والحقول حلقة `0 0 0 3px` من `--ring`، والأزرار `outline: 2px solid var(--ring)`. |
| 7 | العرض عند ٣٧٥ بكسل | **✅ فُحص فعلياً** في الوضعين — القائمة تنكمش إلى أيقونات، ولا تمرير أفقي. |
| 8 | كل نص ظاهر موجود في `naf-terms.md` | **جزئي.** صُحّحت كل المخالفات المباشرة للمصطلحات المسجّلة. لكن المنصة تحوي مئات النصوص التشغيلية (أسماء المزوّدين، شروح التكاملات، رسائل النشر) لا مقابل لها في الملف. لم أفحصها واحداً واحداً. |
| 9 | كل أيقونة موجودة في `naf-icons.md` بالمعنى نفسه | **جزئي.** المستبدَلتان مسجّلتان (`مرفق → Paperclip` · `رجوع → ArrowRight`). لم أراجع الـ ٣٥+ أيقونة القائمة أصلاً مقابل الخريطة — يحتاج تشغيل `/verify-icons`. |
| 10 | لا رقم ولا تاريخ منسَّق سطرياً | **✅ في نطاق `web/src`.** كلها تمرّ بـ `naf-format` الآن. استثناءان مقصودان: مفاتيح `en-CA` للتصفية الزمنية (بيانات لا عرض)، وأسماء الشهور العربية في التقويم ومنتقي التاريخ (لا مقابل لها في `naf-format`). |
| 11 | المبالغ عبر `Money` من `naf-currency` | **لا ينطبق.** المنصة لا تعرض أي مبلغ مالي. لم أركّب `naf-currency`. |

**فحوص إضافية:** `tsc --noEmit` نظيف في الخادم والواجهة · ٤٩ اختباراً آلياً تمرّ · البناء ينجح.

---

## ٧) الحالة النهائية

- **رموز محلية موازية:** من ٢٩ إلى **٧** (كلها لا مقابل لها، وموسومة بذلك في `styles.css`).
- **ملفات السجلّ المنسوخة حرفياً:** `naf-theme.css` · `naf-font.css` · `lib/format.ts` · `lib/utils.ts` · `NafLogo.tsx` · ٥ ملفات شعار. **لم يُعدَّل أيٌّ منها** عدا مسار استيراد `cn` داخل `NafLogo.tsx` (نسخة، لا الأصل).
- **لم يُحذف أي ملف.**

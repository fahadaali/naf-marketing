# سجلّ التغييرات

يُولَّد آلياً من أوصاف الالتزامات. لا يُحرَّر يدوياً.

## 1.10.0 — 2026-08-22

### ميزات

- **campaign:** صفحةُ حملةٍ بمسارٍ مستقلّ، وقائمةٌ تُبحث وتُصفّى وتُفرز (`7193652`)
- **campaign:** مسؤولٌ وميزانيةٌ ومستهدفاتٌ للحملة، ودورةُ حياةٍ يحرسها الخادم (`d1d181e`)

### إصلاحات

- **campaign:** ردٌّ ناقصٌ من الـ API يُقرأ غياباً لا شاشةً بيضاء (`8303dd3`)
- **campaign:** إبرازٌ واحد في المشهد، ورقمُ قضيةٍ لا ينكسر عند ٣٧٥ بكسل (`7134c71`)

## 1.9.1 — 2026-08-20

### إصلاحات

- حفظُ الملفّ المُصدَّر يعمل على آيفون (`f3e5f10`)

## 1.9.0 — 2026-08-19

### ميزات

- **metrics:** خطّ الاتجاه في بطاقة المؤشر (`2a438cf`)
- **workflow:** لا يعتمد المحتوى كاتبُه (`3694188`)

### إصلاحات

- **pwa:** وسما theme-color، وعمودُ آخر ظهورٍ يُكتب ولا يُرى (`8447b6f`)
- **ui:** نوافذ المنصة بدل مربّعات المتصفح — الثلاثة عشر الباقية (`8d807c9`)
- **security:** سرُّ التوقيع خارج ما يُقرأ، وخطّافٌ بلا سرٍّ لا يُستقبَل (`5ac7379`)
- **ui:** نموذجٌ يَعِد بما لا يقع، ومسارٌ مجهول يُقرأ نجاحاً، وبريدٌ يبتلع (`100fc37`)

## 1.8.1 — 2026-08-05

### إصلاحات

- **newsletter:** «لون آخر» صفٌّ بأيقونةٍ ونصّ لا مربّعٌ في آخر الشبكة (`a524747`)

## 1.8.0 — 2026-08-05

### ميزات

- **newsletter:** إخفاء علامات التنسيق، وإعادة اللون إلى الافتراضي، وشريط لوحةٍ للمنتقي (`dbc23b4`)

## 1.7.0 — 2026-08-05

### ميزات

- **newsletter:** طبقة تلوين خلف حقل الفقرة — الكاتب يرى ألوانه لا علامتها (`009d444`)

## 1.6.0 — 2026-08-05

### ميزات

- **newsletter:** نوافذ المنصة بدل مربّعات المتصفح، وأدوات التخصيص والقوالب (`6204a28`)
- **newsletter:** قوالب تُحفظ وتُنشأ منها نشرات، واستيرادها من ملف أو HTML (`d95eafd`)
- **newsletter:** تخصيص ألوان الكتل وكلماتها وسمةٌ للنشرة كاملة (`f469153`)

### إصلاحات

- **deploy:** الهجرات داخل أمر النشر لا خطوةً يتذكّرها إنسان (`3556111`)
- **newsletter:** ما كشفه فحص المسارات والمتصفح — إعداداتٌ لا تُحفظ وفقاعةٌ أجنبية (`d45775c`)
- **newsletter:** تنظيف تخصيص الكتلة عند القراءة والتصيير لا عند الاستيراد وحده (`8501706`)

## 1.5.0 — 2026-08-05

### ميزات

- **newsletter:** توليد PDF في الخادم، وتوسيع التضمين والنشر (`5e71b93`)
- **newsletter:** تصدير المقالة مستند Word ونسخةً للطباعة (`175a3a4`)
- **newsletter:** تدقيق لغوي عربي وبيانات منظّمة للمقالة (`dde3ef5`)
- **newsletter:** كتل الصوت والملف والفيديو والتضمين والأعمدة وقائمة المهام (`fd5cad8`)
- **newsletter:** كتل الحاشية والبطاقة والجدول والفهرس والكود (`a2fdb2e`)
- **newsletter:** حفظ تلقائي ومكتبة وسائط وتفعيل الجدولة (`7835c88`)
- **newsletter:** روابط وتنسيق داخل نصّ الفقرة (`dd58574`)

### إصلاحات

- **newsletter:** ما كشفه تدقيق التغطية — كتلٌ تختفي وفرعٌ ميت وصيغتان (`a262265`)
- **newsletter:** عيوب مستند الطباعة، ومرآة إصلاح الترويسة (`638b38f`)
- **newsletter:** البيانات المنظّمة تتبع نوع الصفحة، والأعمدة تدخل المقتطف (`0489915`)

## 1.4.3 — 2026-08-05

### إصلاحات

- **integrations:** «الموقع الرئيسي» يظهر باسمه، ومسار نشرٍ حين يقف البناء (`a38ee22`)

## 1.4.2 — 2026-08-03

### إصلاحات

- **analytics:** «تسجيل» في دليل المؤشرات يفتح نافذة تُرى (`6066a59`)

## 1.4.1 — 2026-08-03

### إصلاحات

- **metrics:** قراءة اللوحة المختصرة تتجاوز حدّ رابطات D1 (`3e5db5c`)

## 1.4.0 — 2026-08-03

### ميزات

- **metrics:** وصل ثمانية مؤشرات كانت بلا مصدر (`793dd9c`)

## 1.3.0 — 2026-08-01

### ميزات

- إتمام ما بقي من الدليل — المرجعية والقرار والمرحلتان والتقارير (`c9f6f75`)
- إعادة بناء شاشة التحليلات على طبقات الدليل العشر (`e0e6882`)
- احتساب المؤشرات وسحبها من المصادر الخارجية (`7c9ce03`)
- سجلّ المؤشرات — الدليل كاملاً في قاعدة البيانات (`e6efd01`)

### إصلاحات

- «الالتزام بحماية البيانات» مُدخَل لا محتسب (`d0de14b`)

## 1.2.0 — 2026-07-29

### ميزات

- المنصة تُضاف إلى الشاشة الرئيسية وتُفتح كتطبيق (`f641576`)

### إصلاحات

- مطابقة package-lock.json لـ@resvg/resvg-js فيسقط npm ci (`0dec9bd`)

## 1.1.2 — 2026-07-29

### إصلاحات

- الدخول من المركز يدور في تحميلٍ لا ينتهي — قارئ المستخدم فارق الحارس (`2de07a5`)

## 1.1.1 — 2026-07-29

### إصلاحات

- الترويسة تعرض اسم صاحب الحساب وحده (`7f658ae`)

## 1.1.0 — 2026-07-29

### ميزات

- منصة التسويق تأخذ هيكل ناف الموحّد (`7e0d8bf`)

### إصلاحات

- مزامنة ملف الهيكل مع السجلّ — سطرا الحساب والحشو (`436fcdc`)

## 1.0.7 — 2026-07-29

### إصلاحات

- رفعُ تثبيت naf-auth إلى v3.3.0، والفاحصُ يوافقه (`795c2b4`)
- فاحص عقد الدخول كان يسقط على مستودعٍ صحيح — ثلاثة عيوب فيه (`e9c12b7`)
- أصول الصفحات العامة تصل الزائر — المدوّنة كانت بلا أنماط ولا خطوط ولا شعار (`ee91cb2`)

## 1.0.6 — 2026-07-28

### إصلاحات

- مسار إشعار الخروج الخلفي — الخروج من المركز يُنهي الجلسة هنا (`7295164`)

## 1.0.5 — 2026-07-28

### إصلاحات

- الخروج تنقّلٌ كامل لا نداء fetch — فلا يبقى الخارجُ مكانه عند أي فشل (`d3b3c85`)

## 1.0.4 — 2026-07-28

### إصلاحات

- الدخول لا يسقط على عمود password_hash — كل محاولة كانت تُردّ (`b4a3fc6`)

## 1.0.3 — 2026-07-28

### إصلاحات

- ترقية naf-auth إلى v3.1.1 — الدخول يبلّغ المركز بالصلاحية (`e6ae341`)

## 1.0.2 — 2026-07-28

### إصلاحات

- ملف القفل يشير إلى v3.1.0 كما يشير الوصف (`bf3c5d3`)
- أيقونة `Lock` على شاشة «لا صلاحية» (`486f891`)
- الشاشة التي لا يملكها العضو تقول ذلك، ولا تُفتح ثم تفشل (`39fb117`)
- الخروج يخرج بالمستخدم، ولا يعيده إلى الشاشة التي خرج منها (`1a6f3f0`)

## 1.0.1 — 2026-07-28

### إصلاحات

- ربط naf-marketing بـnaf-id على عقد naf-auth v3.0.0 (`e67c85f`)
- نصّ الحالة على الخلفية الهادئة يبلغ AA (`f5b58c4`)
- ربط مساحة AUTH_KV بمعرّفها (`aa1d21d`)
- اللوحة تعالج ٤٠١ و٤٠٣، وشاشة الرفض تطابق السجلّ (`dbe796c`)
- الحارس يسبق كل شيء، وإغلاق طريق الدخول الثاني (`a3c65bd`)
- توقيع التبليغ العكسي وتسجيل حالة ردّ المركز (`89e34be`)
- تثبيت naf-auth على v3.0.0 وإعادة حلّ القفل (`a0b8276`)

## 1.0.0 — 2026-07-27

### تغييرات كاسرة

- رفع lucide-react إلى 1.27.0، وشعارات المنصات الثمانية من السجلّ (`4c66f7a`)

### ميزات

- ربط المنصة بالدخول الموحّد عبر naf-id (#31) (`48a999c`)
- التبليغ العكسي عند إيقاف عضو، ومعالجة انحرافات شاشة الرفض (`e91d898`)
- ربط المنصة بالدخول الموحّد عبر naf-id (`6683841`)
- شعارات المنصات من naf-brand-marks، وتثبيت lucide يبقى حتى يُحسم لينكدإن (`a46c16d`)
- استهلاك رموز السجلّ — سلّم النصّ والمسافات والعلامات الخارجية (`df596cd`)
- توحيد أيقونة التوليد على Sparkles، وحمولة السجلّ جاهزة (`2b43a59`)

### إصلاحات

- مصدر واحد للاتجاه، وعزل المصطلحات اللاتينية داخل النص العربي (`a4dc5df`)
- عرض التواريخ من lib/format لا بـIntl سطرياً (`d675fdb`)
- قيم قالب البريد في ملف واحد، وهويته هوية ناف لا نيليّة (`f790d64`)
- نصوص الحالات والرسائل والشاشات الفارغة من naf-terms (`adb85bd`)
- تثبيت lucide بالضبط، وإزالة معالجات لونية غير مسجّلة (`1c2815c`)
- حالة تركيز ظاهرة، وأهداف النقر تُشغَّل بلوحة المفاتيح (`0ff88c4`)
- عزل كل رقم اتجاهياً، وأرقام جدولية في الجداول والمؤشّرات (`e0a4f4e`)
- قلب الأسهم والشيفرون، وأيقونات السجلّ، ومقاسات الأيقونات الثلاثة (`29400b0`)
